import { FiltroNumeroPipe } from './filtro-numero.pipe';

describe('FiltroNumeroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNumeroPipe();
    expect(pipe).toBeTruthy();
  });
});
